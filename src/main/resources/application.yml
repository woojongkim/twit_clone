spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: auto_woody
    password: woody

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

  devtools:
    livereload:
      enabled: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
#        type:
#          descriptor:
#            sql:
#              BasicBinder: trace